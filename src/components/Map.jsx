import { Grid } from "@mui/material";
import { useState } from "react";

const Map = () => {
    const [rows, setRows] = useState(21);
    return (
        <Grid container>
            
        </Grid>
    );
};

export default Map;